/** Scale for ILS (New Israeli Shekel): 2 decimal places (agorot). */
export const DEFAULT_DECIMAL_SCALE = 2;

/** Cross-rate base currency for multi-currency conversion (future use). */
export const CROSS_RATE_BASE_CURRENCY = 'USD' as const;
