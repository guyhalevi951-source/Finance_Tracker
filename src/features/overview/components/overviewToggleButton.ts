import { SEMANTIC_COLORS } from '../../../config/semanticColors';

export const OVERVIEW_TOGGLE_BUTTON_CLASS = `flex min-h-[44px] min-w-[44px] flex-col items-center justify-start px-2 pt-1 pb-1.5 text-xs font-medium ${SEMANTIC_COLORS.interactive.text}`;

export const OVERVIEW_TOGGLE_FRAME_CLASS =
  'inline-flex overflow-hidden rounded-xl border border-slate-300 dark:border-slate-600';
